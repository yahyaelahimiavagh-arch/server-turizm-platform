# v0.3.2 Private Candidate Pilot Patch

## Install acceptance

- Plugin header reports v0.3.2.
- Dashboard says `PRIVATE CANDIDATE GATE` and `Public writes = 0`.
- Existing `/umre-1/`, legacy HTML generator and Google Sheets menus remain unchanged.
- Hotel Directory remains `9 / READY / Writes 0`.

## 35-program batch

1. Open **Candidate Import** and upload the already accepted 35-program JSON.
2. Stage only. Expected first plan: `Create 35`, `Blocked 0`, warnings may show the source's missing verification timestamps.
3. Confirm the review checkbox and commit private candidates.
4. Expected result: `Created 35`, `Updated 0`, `Unchanged 0`, `Conflicts 0`.
5. Open **Programs**. IDs must be unique and sequentially allocated in `STP-*` format; all records start as `needs_review`.
6. Review Program 219 and Cairo-linked `Kahire3`; confirm dates, prices and resolved Hotel names.
7. Confirm bulk approval. Expected: all READY candidates become `approved`, with no public URL.

## Idempotency and update

1. Stage the identical 35-program JSON again. Expected: `Unchanged 35`.
2. Do not commit a changed source yet. Change one price in a copy of Program 219 JSON and Stage it.
3. Expected: Program 219 resolves to the same `STP-*` target and reports `UPDATE_CANDIDATE`, never CREATE.
4. An unknown externally supplied `STP-*`, duplicate source locator or missing source locator must report `CONFLICT`.

## Lifecycle and archive

- The current sold-out `Özel / ÜÇLER ELEKTRİK` candidate must show `availability = sold_out`, pure `Temporal = upcoming`, and composite `Effective state = sold_out` while its dates are still in the future.
- Upcoming programs do not appear in Archive Candidates.
- Completed or cancelled programs appear automatically as review candidates, but are not archived automatically.
- Explicit Archive creates a historical payload + Hotel snapshot and an audit event.
- Restore returns the record to `needs_review`; no delete action exists.

## v0.3.2 temporal semantics regression

1. Open `STP-000001` Program 219. Expected pure Temporal: `upcoming`; Effective state should also be `upcoming` while availability is open.
2. Open `STP-000019` / `Özel`. Expected workflow availability: `sold_out`; pure Temporal: `upcoming`; Effective state: `sold_out`.
3. Programs table must expose **Temporal** and **Effective state** as separate columns.
4. Canonical stored JSON must remain unchanged by this display-only patch.
5. No public Program route, renderer, SEO/schema output, sitemap entry or `/umre-1/` change may appear.


## Stop conditions

Stop the pilot if any public Program URL appears, `/umre-1/` changes, a repeated batch creates duplicates, an Hotel ID becomes unresolved, or any missing source row is automatically archived/deleted.
