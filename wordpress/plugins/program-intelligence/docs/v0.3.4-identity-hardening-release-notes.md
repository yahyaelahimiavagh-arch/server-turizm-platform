# v0.3.4 — Stable-ID Identity Hardening + Controlled Repair

- Blocks source-row identity drift before an existing Stable-ID Program can be overwritten.
- Adds administrator-only `Identity Repair` with staged preflight, exact-ID mapping, rollback snapshots and zero delete paths.
- Repair saves the canonical source hash separately so the same Google Sheets export becomes UNCHANGED after repair.
- Existing Stable IDs are preserved; fixtures STP-000036/37 are permanently protected.
- Public publishing registry is not mutated by this plugin. Publishing Integration must refresh accepted public route hashes after a repair.
