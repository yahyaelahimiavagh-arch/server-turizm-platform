# Server Turizm Program Intelligence v0.3.1 — Candidate Release Notes

## Change ID
`CHG-P7A-V031-TEMPORAL-SEMANTICS`

## Status
`CANDIDATE — runtime acceptance required`

## Baseline
Built directly from accepted runtime baseline `v0.3.0` Private Candidate Store.

## Scope
Admin-only semantics correction discovered during the sold-out Program review gate.

### Program Review
- `Temporal state` now renders `STPI_Lifecycle::temporal_state()`.
- Composite `Effective state` remains visible as contextual information.
- Availability remains a separate workflow axis.

### Programs table
- `Temporal` now renders the pure temporal axis.
- A separate `Effective state` column shows the composite state.

## Expected sold-out behavior
For a future sold-out Program such as `STP-000019` / `Özel`:
- Editorial: `needs_review`
- Schedule: `scheduled`
- Availability: `sold_out`
- Temporal: `upcoming`
- Effective state: `sold_out`

## Explicitly unchanged
- ST-TDE JSON Schema `1.0.0`
- canonical stored Program payloads
- Stable `STP-*` IDs
- Hotel `STH-*` relations
- Candidate Import planning/commit behavior
- source-row idempotency
- revisions and Audit Log
- approval/archive/restore logic
- no-delete policy
- no public Program renderer
- SEO/schema/sitemap output remains absent
- `/umre-1/` remains untouched

## Runtime acceptance checklist
1. Upgrade v0.3.0 to v0.3.1 without deleting Program data.
2. Confirm Program Intelligence dashboard loads and still reports private candidate mode / public writes 0.
3. Confirm Programs table shows separate `Temporal` and `Effective state` columns.
4. Open `STP-000001` / Program 219:
   - stable ID unchanged;
   - editorial remains `approved`;
   - double/triple/quad prices remain `2400 / 2350 / 2300 USD`;
   - Temporal = `upcoming`;
   - Effective state = `upcoming`.
5. Open `STP-000019` / `Özel`:
   - editorial = `needs_review`;
   - schedule / availability = `scheduled / sold_out`;
   - Temporal = `upcoming`;
   - Effective state = `sold_out`.
6. Do not approve `STP-000019` until the display regression is accepted.
7. Confirm `/umre-1/` and current public rendering remain unchanged.
8. If any regression appears, rollback to v0.3.0.

## Static release verification
- PHP lint: PASS
- JSON examples/schema validation: PASS
- private CPT / staged import / human confirmation: PASS
- allocator lock / idempotency: PASS
- archive snapshot / audit: PASS
- no public renderer / no delete path: PASS
- temporal/effective admin semantics regression: PASS
- Google Sheets exporter test: PASS
- direct lifecycle harness: future sold-out => `temporal=upcoming`, `effective=sold_out`: PASS

## Rollback target
`server-turizm-program-intelligence-v0.3.0-private-candidate.zip`
