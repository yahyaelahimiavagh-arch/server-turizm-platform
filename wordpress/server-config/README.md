# Server Configuration

`.htaccess` is an exact 2026-09-14 runtime snapshot. Preserve WordPress and
LiteSpeed managed blocks and keep the exact Porto directory guard outside those
managed regions.

Do not deploy this file blindly to another environment; compare host-specific
managed blocks first.
